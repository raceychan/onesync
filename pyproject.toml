[tool.poetry]
name = "onlysync"
version = "0.1.0"
description = "A cloud-driven linux deps and config management tool"
authors = ["raceychan <raceychan@gmail.com>"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "3.11"
typer = "0.9.0"
loguru = "^0.7.0"
textual = "^0.22.3"
pytest = "^7.3.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
